# Polices de l'application

Déposer ici les fichiers `.ttf` / `.otf` de la web app, déclarer le bloc
`fonts:` dans `pubspec.yaml`, puis renseigner `CapType.fontFamily` dans
`lib/design/tokens.dart`.

Tant que ce dossier est vide, l'app utilise la police système.
