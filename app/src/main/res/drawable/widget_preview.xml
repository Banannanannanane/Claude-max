<?xml version="1.0" encoding="utf-8"?>
<!--
  Placeholder shown in the widget picker and for the split second before the first
  bitmap lands. The real room is drawn by WidgetRenderer.
-->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@color/tbh_stone" />
            <stroke android:width="2dp" android:color="@color/tbh_bevel_light" />
        </shape>
    </item>
    <item android:gravity="center" android:width="10dp" android:height="10dp">
        <shape android:shape="rectangle">
            <solid android:color="@color/tbh_gold" />
        </shape>
    </item>
</layer-list>
